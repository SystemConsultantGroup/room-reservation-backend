spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true

logging:
  level:
    org.springframework: DEBUG
    org.hibernate.orm.jdbc.bind: DEBUG

cookie:
  secure: false